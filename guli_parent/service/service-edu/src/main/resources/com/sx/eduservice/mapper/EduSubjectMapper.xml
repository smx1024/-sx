<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sx.eduservice.mapper.EduSubjectMapper">

    <resultMap type="com.sx.eduservice.entity.EduSubject" id="EduSubjectMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="parentId" column="parent_id" jdbcType="VARCHAR"/>
        <result property="sort" column="sort" jdbcType="VARCHAR"/>
        <result property="gmtCreate" column="gmt_create" jdbcType="TIMESTAMP"/>
        <result property="gmtModified" column="gmt_modified" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into guliedu.edu_subject(title, parent_id, sort, gmt_create, gmt_modified)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.title}, #{entity.parentId}, #{entity.sort}, #{entity.gmtCreate}, #{entity.gmtModified})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into guliedu.edu_subject(title, parent_id, sort, gmt_create, gmt_modified)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.parentId}, #{entity.sort}, #{entity.gmtCreate}, #{entity.gmtModified})
        </foreach>
        on duplicate key update
         title = values(title) , parent_id = values(parent_id) , sort = values(sort) , gmt_create = values(gmt_create) , gmt_modified = values(gmt_modified)     </insert>

</mapper>

